{
  "name": "minhas-vendas-saas",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/backend",
    "apps/web"
  ],
  "scripts": {
    "format": "npm run format --workspaces",
    "lint": "npm run lint --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "dev": "npm run dev --workspaces",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.1.4"
  }
}